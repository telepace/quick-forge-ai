# 文档

欢迎阅读Quick Forge AI的文档。

## 安装

```bash
npm install quick-forge-ai
```

## 使用

```javascript
import { QuickForge } from 'quick-forge-ai'

const ai = new QuickForge({
  apiKey: 'your-api-key'
})

// 使用AI
const response = await ai.generate('你的提示')
console.log(response)
``` 